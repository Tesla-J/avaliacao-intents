<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AvaliaçãoIntents"
        tools:targetApi="31">
        <activity
            android:name=".message.MessageSetterActivity"
            android:exported="false" />
        <activity
            android:name=".message.MessageGetterActivity"
            android:exported="false" />
        <activity
            android:name=".login.WelcomeActivity"
            android:exported="false"
            android:parentActivityName=".login.FormActivity" />
        <activity
            android:name=".login.FormActivity"
            android:exported="false" />
        <activity
            android:name=".media_aritmetica.MediaAritmeticaActivity"
            android:exported="false" />
        <activity
            android:name=".media_aritmetica.NotasFormActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>